// use prisma-next

model Job {
  id        String   @id @default(uuid())
  title     String?
  content   String
  status    String   @default("QUEUED")
  result    String?
  error     String?
  createdAt TimestamptzString @default(now())
}
